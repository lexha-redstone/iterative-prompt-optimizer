You are an absolute master pixel artist and spatial infographic designer. Generate a strict, authentic 1-bit style (pure black and pure white ONLY) isometric pixel art infographic visually representing the data provided in the DATA_TO_TRANSCRIBE block below.

You must meticulously execute this image by obeying the following absolute structural, spatial, and aesthetic mandates:

1. [THE UNIFIED GLOBAL PIXEL GRID]
- Scale Mandate: The ENTIRE image MUST operate on a single, unified, mathematically uniform global pixel grid. Every single line, text character, building, prop, and dither pattern must snap to the exact same 1x1 pixel scale.
- Anti-Mixel Rule: ABSOLUTELY ZERO "mixels" or varying resolutions. You are strictly forbidden from using massive blocky faux-pixels for macro headers alongside microscopic high-res pixels for details. 
- Edge Rule: ALL elements must be strictly aliased. Ban all smooth lines, vector paths, and anti-aliasing. Every edge must be distinctly jagged and stair-stepped.

2. [TYPOGRAPHY & DATA TRANSCRIPTION]
DATA_TO_TRANSCRIBE:
"&USER_INPUT&"
- Transcription Rule: You MUST ONLY transcribe the exact text provided above. Heavily summarize into punchy 1-3 word macro-labels to preserve the low-res aesthetic.
- Absolute Prohibition on Hallucination: Under NO CIRCUMSTANCES should you hallucinate, infer, or insert extra descriptive text, secondary subtitles, or stray letters.
- Single Instance Rule: Never duplicate labels. Transcribe and place each label exactly ONCE per isolated concept. Do not spam labels above and below the same object.
- Font Mandate: All typography MUST be rendered natively as jagged, aliased pixel art on the global pixel grid. Smooth, anti-aliased, or standard vector fonts are strictly banned.

3. [MODULAR ISLAND LAYOUT & TRUE 3D VOLUMETRICS]
- Spatial Separation: Construct a modular layout where each discrete data point is assigned its own physically independent 3D platform. Islands must float in a vast, empty expanse of solid-color negative space (no spaghetti architecture or connecting tangles).
- 3D Extrusion Mandate: Every object, prop, and platform inside the isometric grid must be built from solid, extruded 3D isometric blocks natively locked to a 30-degree projection.
- Internal Flow Rule: Internal flow markers or directional arrows MUST be rendered as 3D volumetric extruded objects. Flat 2D decals or arrows pasted flatly onto angled 3D ground planes are strictly prohibited.

4. [STRICT 3-TONE LIGHTING & 1-BIT DITHERING]
- Shading Rule: Establish true spatial volume across ALL 3D structures (buildings, servers, small props) by enforcing a strict flat 3-tone lighting model: Highlight (Pure White) on the Top face, Midtone on the Left face, and Core Shadow (Pure Black) on the Right face. 
- Anti-Line-Art Rule: Ban flat, unshaded 2D wireframe structures or transparent boxes. Everything must have solid geometric volume.
- Dithering Mandate: To create midtones, EXCLUSIVELY use authentic, grid-aligned 1x1 pixel checkerboard patterns (Bayer dithering) placed pixel-by-pixel on the global grid.
- Prohibited Effects: Absolutely no global noise filters, vector screentones, angled halftone dot patterns, soft drop shadows, grayscale, or smooth gradients. 

5. [THE 2D HUD ISOLATION RULE]
- Orthographic Margins: To maintain perfect readability, all typography (labels, titles, legends) must be treated as flat 2D HUD overlays perfectly facing the camera.
- Spatial Boundaries: You must anchor all flat 2D HUD text exclusively within the pure negative space along the outer margins of the image, completely outside the isometric geometry.
- No Intersections: 2D HUD elements must never physically intersect, hover directly over, or be mapped/skewed onto the angled 3D isometric planes. Maintain absolute physical separation.