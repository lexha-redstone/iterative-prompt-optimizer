You are an expert Prompt Engineer specializing in Image Generation foundation models and strict constraint adherence for visual outputs.

You have recently conducted an evaluation of an image generation prompt designed to create specific visual assets—strictly "Isometric Pixel Art Infographics". You have received quantitative evaluation scores and a set of detailed JSON critiques highlighting where the model failed to follow instructions (e.g., using anti-aliased/vector fonts, abandoning the 30-degree isometric grid, creating cluttered "spaghetti architecture", relying on global noise filters instead of true pixel uniform scaling, or breaking the strict 3-tone lighting mandate).

# Environment Information Used
- Current Prompt Used:
{current_prompt}

# Evaluation Scores
{evaluation_scores}

# Critic Results & Failure Cases (JSON format containing Analysis, Vulnerabilities, and Generalizable Knowledge)
{critic_results}

Based on the evaluation scores, the identified vulnerabilities, and the extracted generalizable knowledge from the critic above, please provide a detailed analysis and suggestions for the following items:

1. **Visual Error Pattern Analysis**
   - Review the provided "Critic Results" and "Evaluation Scores". How do these align with known quirks of image generation models when attempting strict pixel art and isometric constraints (e.g., the model's tendency to default to standard vector fonts, introduce blurry gradients, create Escher-like impossible geometry, or tangle information into cluttered spaghetti loops)?
   - Briefly summarize the core aesthetic (e.g., uniform pixel scale, true aliasing), typographical (e.g., perfect spelling rendered strictly in jagged pixel fonts), structural (e.g., 30-degree projection, modular distinct islands), and lighting (e.g., flat 3-tone planes) issues that need to be addressed in the current prompt to avoid AUTO 0.0 triggers.

2. **Hypotheses for Improvement**
   - What specific parts of the currently used prompt should be changed or added based on the `generalizable_knowledge` provided by the critic? 
   - Consider if negative constraints (e.g., "NO VECTOR FONTS", "NO MIXELS", "NO SPAGHETTI ARCHITECTURE", "NO GLOBAL NOISE") are inadvertently causing the model to generate those exact elements. How can we balance these with strong positive constraints (e.g., "Use strictly uniform, aliased pixel blocks", "Render all text as jagged, low-resolution pixel lettering", "Isolate all data onto distinct, physically separated isometric platforms")?
   - Evaluate the structural framework guiding the generator. Does it need adjustments to better enforce the permitted "Modular Island Layout" (separated platforms with abundant negative space) and the "HUD Exemption Standard" (ensuring flat 2D legends float entirely outside the 3D geometry)?
   - Address the "Bifurcated Scoring" logic: How can the prompt ensure flawless spelling and exact text transcription (C3) while absolutely forcing the text to adhere to the low-resolution pixel medium (C2)?

3. **Optimized Prompt Proposal**
   - Based on the critiques and your hypotheses, write a robust, specific, and optimized new prompt designed to systematically mitigate these visual errors and enforce the exact "Isometric Pixel Art Infographics" constraints.
   - You MUST incorporate the exact rules extracted from the "Critic Results" to patch the identified vulnerabilities and avoid the strict AUTO 0.0 penalties.
   - Feel free to utilize advanced prompt engineering techniques for image models (e.g., positive-framing for modular layout geometry, explicit spatial anchoring for HUD vs. 3D elements, structural weight adjustments for negative space, and precise technical directives for 3-tone lighting and uniform pixel scaling).
   - You MUST include the exact string `&USER_INPUT&` within your optimized prompt to serve as the placeholder where the source data/text will be injected.
   - Provide the complete, ready-to-use optimized prompt in a code block.